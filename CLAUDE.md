## 🧠 記憶與同步鐵律（不用等我開口，你自己做）

所有記憶檔一律寫在這個專案的 .claude/memory/ 底下，不要寫到別的地方。

### 1. 做完會影響別的對話視窗的事 → 自己貼公告

完成下列任一件事，立刻自己 append 一條到 .claude/memory/SYNC_BOARD.md 的
「## 📌 最新公告」最上面，格式：`- [YYYY-MM-DD | 類別 | 來源] 一句話結論`。

該貼的：價格異動、成交或退款、系統上線或修好的 bug、我拍板的決策、對客戶的話術改動、
新增的禁忌、行銷檔期、外部平台出事。

判準只有一句：**「別的對話視窗不知道這件事，會不會害它做錯或答錯客戶？」**
會 → 貼；不會 → 不要貼（否則公告欄變成沒人看的雜訊）。公告只放「結論」，不放「過程」。

### 2. 我隨口交代的事 → 自己記進待辦

我說「這個之後再做」「下週處理」「記得幫我…」→ 立刻 append 到
.claude/memory/TODO_BOARD.md 的「## 🔲 未完成」。做完把 `- [ ]` 改成 `- [x]`。不用問我要不要記。

### 3. 討論出結論 → 自己存成記憶檔（但要過門檻）

**先過第一道**：這件事反覆討論了 3 輪以上？我講了「以後都這樣」「這是鐵律」「拍板」？
下次開新對話還會用到？──有一個成立才存，1 問 1 答的閒聊不要存。

**再過第二道（畢業三問，三個都要 Yes）**：
① 拿掉這次的偶然細節後還成立嗎？
② 下個月碰到類似情況，我會想翻出來嗎？
③ 這是「結論」不是「過程」嗎？

有一個不過 → 留在對話裡，不要寫檔。

存的時候：在 .claude/memory/ 建一個檔，檔名用
`feedback_主題.md`（我拍板的規矩）／`learning_主題.md`（踩過的坑）／
`reference_主題.md`（永久參考資料）／`project_主題.md`（進行中的專案狀態），
然後在 MEMORY.md 加一行索引指過去。

存完只回我一行：`已存 X｜一句話結論｜不要就說「拿掉」`。
不要問我要不要存，直接做，讓我兩秒能否決就好。

### 4. 對話中問出來、會改變以後判斷的答案 → 當場回寫

不要等下次想起來。想不起來的，就永遠散在對話記錄裡了。

---

## 📇 長期記憶索引（開對話時自動載入）

@.claude/memory/MEMORY.md
